<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:orientation="vertical">

    <ImageView android:id="@+id/grid_image"
               android:layout_width="160dp"
               android:layout_height="160dp"
               android:background="@drawable/bordered_drawable"
               android:scaleType="centerCrop"
               android:gravity="center"/>

    <TextView android:id="@+id/grid_label"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_below="@id/grid_image"
              android:layout_centerHorizontal="true"
              android:layout_marginBottom="20dp"
              android:text="@string/grid_lbl_txt"
              android:textColor="@color/solid_black"/>

    <ImageView
            android:id="@+id/grid_x_mark"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_alignBottom="@id/grid_image"
            android:layout_alignRight="@id/grid_image"
            android:src="@drawable/x_mark"
            android:visibility="invisible"
            android:layout_margin="2dp"/>

</RelativeLayout>